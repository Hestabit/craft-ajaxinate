{% macro input(type = "text", name, value, fieldType, class ) %}
    <input type="{{ type }}" name="{{ name }}" value="{{ value|e }}" class="{{ class }}"  data-ftype="{{ fieldType}}" />
{% endmacro %}